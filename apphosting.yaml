runConfig:
  runtime: nodejs20

scripts:
  buildCommand: npm ci && npm run build
  runCommand: node .next/standalone/server.js

outputFiles:
  serverApp:
    include:
      - .next
      - public
      - package.json
      - next.config.js
